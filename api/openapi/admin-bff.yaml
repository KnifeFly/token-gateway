openapi: 3.0.3
info:
  title: token-gateway Admin Web BFF
  version: 0.2.0
  description: Browser-facing Admin BFF contract owned by cmd/console. Admin Web uses operator sessions, RBAC, CSRF, idempotency metadata, and durable audit instead of exposing /admin/* machine APIs to browsers.
servers:
  - url: http://localhost:9505
    description: Local console process
tags:
  - name: Admin Web Auth
  - name: Admin Web Dashboard
  - name: Admin Web Config
  - name: Admin Web Snapshots
  - name: Admin Web Operations
  - name: Admin Web Audit
paths:
  /api/admin/v1/auth/login:
    post:
      tags: [Admin Web Auth]
      summary: Exchange operator credentials for an HttpOnly Admin session
      operationId: loginAdmin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminLoginRequest"
      responses:
        "200":
          description: Admin session and CSRF token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminLoginResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/auth/logout:
    post:
      tags: [Admin Web Auth]
      summary: Revoke the current Admin session
      operationId: logoutAdmin
      security:
        - adminSession: []
          csrfHeader: []
      responses:
        "200":
          description: Session revoked
          content:
            application/json:
              schema:
                type: object
                properties:
                  authenticated:
                    type: boolean
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/auth/me:
    get:
      tags: [Admin Web Auth]
      summary: Get the current Admin operator session
      operationId: getAdminSession
      security:
        - adminSession: []
      responses:
        "200":
          description: Admin operator session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminSessionResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/dashboard:
    get:
      tags: [Admin Web Dashboard]
      summary: Get Admin dashboard counters
      operationId: getAdminDashboard
      security:
        - adminSession: []
      responses:
        "200":
          description: Admin dashboard counters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminDashboardResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/tenants:
    get:
      tags: [Admin Web Config]
      operationId: listAdminTenants
      security:
        - adminSession: []
      responses:
        "200":
          $ref: "#/components/responses/ListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
    post:
      tags: [Admin Web Config]
      operationId: upsertAdminTenant
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      requestBody:
        $ref: "#/components/requestBodies/JSONBody"
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/tenants/{tenant_id}:
    get:
      tags: [Admin Web Config]
      operationId: getAdminTenant
      security:
        - adminSession: []
      parameters:
        - $ref: "#/components/parameters/TenantID"
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/projects:
    get:
      tags: [Admin Web Config]
      operationId: listAdminProjects
      security:
        - adminSession: []
      parameters:
        - $ref: "#/components/parameters/TenantIDQuery"
      responses:
        "200":
          $ref: "#/components/responses/ListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
    post:
      tags: [Admin Web Config]
      operationId: upsertAdminProject
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      requestBody:
        $ref: "#/components/requestBodies/JSONBody"
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/api-keys:
    get:
      tags: [Admin Web Config]
      operationId: listAdminAPIKeys
      security:
        - adminSession: []
      parameters:
        - $ref: "#/components/parameters/TenantIDQuery"
        - $ref: "#/components/parameters/ProjectIDQuery"
      responses:
        "200":
          $ref: "#/components/responses/ListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
    post:
      tags: [Admin Web Config]
      operationId: createAdminAPIKey
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      requestBody:
        $ref: "#/components/requestBodies/JSONBody"
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/api-keys/{key_id}/disable:
    post:
      tags: [Admin Web Config]
      operationId: disableAdminAPIKey
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      parameters:
        - $ref: "#/components/parameters/KeyID"
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/models:
    get:
      tags: [Admin Web Config]
      operationId: listAdminModels
      security:
        - adminSession: []
      responses:
        "200":
          $ref: "#/components/responses/ListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
    post:
      tags: [Admin Web Config]
      operationId: upsertAdminModel
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      requestBody:
        $ref: "#/components/requestBodies/JSONBody"
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/channels:
    get:
      tags: [Admin Web Config]
      operationId: listAdminChannels
      security:
        - adminSession: []
      responses:
        "200":
          description: Safe channel list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminChannelListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
    post:
      tags: [Admin Web Config]
      operationId: upsertAdminChannel
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminChannelUpsertRequest"
      responses:
        "200":
          description: Safe channel read model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminChannelView"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/channels/{channel_id}:
    get:
      tags: [Admin Web Config]
      operationId: getAdminChannel
      security:
        - adminSession: []
      parameters:
        - $ref: "#/components/parameters/ChannelID"
      responses:
        "200":
          description: Safe channel read model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminChannelView"
        default:
          $ref: "#/components/responses/ErrorResponse"
    patch:
      tags: [Admin Web Config]
      operationId: patchAdminChannel
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      parameters:
        - $ref: "#/components/parameters/ChannelID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminChannelUpsertRequest"
      responses:
        "200":
          description: Safe channel read model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminChannelView"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/channels/{channel_id}/test:
    post:
      tags: [Admin Web Config]
      operationId: testAdminChannel
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      parameters:
        - $ref: "#/components/parameters/ChannelID"
      responses:
        "200":
          description: Safe channel test result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminChannelTestResult"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/channels/{channel_id}/rotate-credential:
    post:
      tags: [Admin Web Config]
      operationId: rotateAdminChannelCredential
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      parameters:
        - $ref: "#/components/parameters/ChannelID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminChannelCredentialRotationRequest"
      responses:
        "200":
          description: Safe channel read model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminChannelView"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/channels/{channel_id}/sync-preview:
    post:
      tags: [Admin Web Config]
      operationId: previewAdminChannelSync
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      parameters:
        - $ref: "#/components/parameters/ChannelID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminChannelSyncRequest"
      responses:
        "200":
          description: Non-persistent channel model sync preview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminChannelSyncPreview"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/channels/{channel_id}/sync-apply:
    post:
      tags: [Admin Web Config]
      operationId: applyAdminChannelSync
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      parameters:
        - $ref: "#/components/parameters/ChannelID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminChannelSyncRequest"
      responses:
        "200":
          description: Persisted channel model sync result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminChannelSyncApplyResult"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/channels/{channel_id}/health-events:
    get:
      tags: [Admin Web Config]
      operationId: listAdminChannelHealthEvents
      security:
        - adminSession: []
      parameters:
        - $ref: "#/components/parameters/ChannelID"
      responses:
        "200":
          description: Safe synthetic channel health events
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminChannelHealthEventListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/channels/{channel_id}/disable:
    post:
      tags: [Admin Web Config]
      operationId: disableAdminChannel
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      parameters:
        - $ref: "#/components/parameters/ChannelID"
      responses:
        "200":
          description: Safe channel read model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminChannelView"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/channels/{channel_id}/enable:
    post:
      tags: [Admin Web Config]
      operationId: enableAdminChannel
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      parameters:
        - $ref: "#/components/parameters/ChannelID"
      responses:
        "200":
          description: Safe channel read model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminChannelView"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/routes:
    get:
      tags: [Admin Web Config]
      operationId: listAdminRoutes
      security:
        - adminSession: []
      responses:
        "200":
          $ref: "#/components/responses/ListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
    post:
      tags: [Admin Web Config]
      operationId: upsertAdminRoute
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      requestBody:
        $ref: "#/components/requestBodies/JSONBody"
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/pricing:
    get:
      tags: [Admin Web Config]
      operationId: listAdminPricing
      security:
        - adminSession: []
      responses:
        "200":
          $ref: "#/components/responses/ListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
    post:
      tags: [Admin Web Config]
      operationId: upsertAdminPricing
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      requestBody:
        $ref: "#/components/requestBodies/JSONBody"
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/limits:
    get:
      tags: [Admin Web Config]
      operationId: listAdminLimits
      security:
        - adminSession: []
      responses:
        "200":
          $ref: "#/components/responses/ListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
    post:
      tags: [Admin Web Config]
      operationId: upsertAdminLimit
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      requestBody:
        $ref: "#/components/requestBodies/JSONBody"
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/snapshots:
    get:
      tags: [Admin Web Snapshots]
      operationId: getAdminSnapshots
      security:
        - adminSession: []
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/snapshots/validate:
    post:
      tags: [Admin Web Snapshots]
      operationId: validateAdminSnapshot
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/snapshots/publish:
    post:
      tags: [Admin Web Snapshots]
      operationId: publishAdminSnapshot
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/snapshots/rollback:
    post:
      tags: [Admin Web Snapshots]
      operationId: rollbackAdminSnapshot
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/operations/settlements:
    get:
      tags: [Admin Web Operations]
      operationId: listAdminSettlements
      security:
        - adminSession: []
      responses:
        "200":
          $ref: "#/components/responses/ListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/operations/settlements/{id}/replay:
    post:
      tags: [Admin Web Operations]
      operationId: replayAdminSettlement
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/operations/callbacks:
    get:
      tags: [Admin Web Operations]
      operationId: listAdminCallbacks
      security:
        - adminSession: []
      parameters:
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          $ref: "#/components/responses/ListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/operations/callbacks/{id}/retry:
    post:
      tags: [Admin Web Operations]
      operationId: retryAdminCallback
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/operations/workers:
    get:
      tags: [Admin Web Operations]
      operationId: listAdminWorkers
      security:
        - adminSession: []
      responses:
        "200":
          $ref: "#/components/responses/ListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/operations/holds:
    get:
      tags: [Admin Web Operations]
      operationId: listAdminHolds
      security:
        - adminSession: []
      responses:
        "200":
          $ref: "#/components/responses/ListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/audit:
    get:
      tags: [Admin Web Audit]
      operationId: listAdminAudit
      security:
        - adminSession: []
      parameters:
        - name: operator_id
          in: query
          schema: { type: string }
        - name: action
          in: query
          schema: { type: string }
        - name: resource
          in: query
          schema: { type: string }
        - name: from
          in: query
          schema: { type: string, format: date-time }
        - name: to
          in: query
          schema: { type: string, format: date-time }
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          $ref: "#/components/responses/ListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/operators:
    get:
      tags: [Admin Web Audit]
      operationId: listAdminOperators
      security:
        - adminSession: []
      responses:
        "200":
          $ref: "#/components/responses/ListResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
    post:
      tags: [Admin Web Audit]
      operationId: createAdminOperator
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      requestBody:
        $ref: "#/components/requestBodies/JSONBody"
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
  /api/admin/v1/operators/{operator_id}/disable:
    post:
      tags: [Admin Web Audit]
      operationId: disableAdminOperator
      security:
        - adminSession: []
          csrfHeader: []
          idempotencyKey: []
          reasonHeader: []
      parameters:
        - $ref: "#/components/parameters/OperatorID"
      responses:
        "200":
          $ref: "#/components/responses/ObjectResponse"
        default:
          $ref: "#/components/responses/ErrorResponse"
components:
  securitySchemes:
    adminSession:
      type: apiKey
      in: cookie
      name: tg_admin_session
    csrfHeader:
      type: apiKey
      in: header
      name: X-CSRF-Token
    idempotencyKey:
      type: apiKey
      in: header
      name: Idempotency-Key
    reasonHeader:
      type: apiKey
      in: header
      name: X-Reason
  parameters:
    TenantID:
      name: tenant_id
      in: path
      required: true
      schema: { type: string }
    TenantIDQuery:
      name: tenant_id
      in: query
      schema: { type: string }
    ProjectIDQuery:
      name: project_id
      in: query
      schema: { type: string }
    KeyID:
      name: key_id
      in: path
      required: true
      schema: { type: string }
    ChannelID:
      name: channel_id
      in: path
      required: true
      schema: { type: string }
    OperatorID:
      name: operator_id
      in: path
      required: true
      schema: { type: string }
    ID:
      name: id
      in: path
      required: true
      schema: { type: string }
    Limit:
      name: limit
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 500
  requestBodies:
    JSONBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true
  responses:
    ObjectResponse:
      description: JSON object response
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true
    ListResponse:
      description: List response
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ListResponse"
    ErrorResponse:
      description: Stable console error envelope
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
  schemas:
    AdminLoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
    AdminLoginResponse:
      type: object
      required: [authenticated, session, csrf_token]
      properties:
        authenticated:
          type: boolean
        session:
          $ref: "#/components/schemas/AdminSessionResponse"
        csrf_token:
          type: string
    AdminSessionResponse:
      type: object
      required: [authenticated]
      properties:
        authenticated:
          type: boolean
        operator_id:
          type: string
        email:
          type: string
        display_name:
          type: string
        roles:
          type: array
          items:
            type: string
        permissions:
          type: array
          items:
            type: object
            properties:
              action:
                type: string
              resource:
                type: string
        expires_at:
          type: string
          format: date-time
        last_seen_at:
          type: string
          format: date-time
        csrf_token:
          type: string
    AdminDashboardResponse:
      type: object
      properties:
        generated_at:
          type: string
          format: date-time
        counts:
          type: object
          additionalProperties:
            type: integer
    ListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            type: object
            additionalProperties: true
    AdminChannelListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/AdminChannelView"
    AdminChannelView:
      type: object
      required: [id, provider_type, base_url, credential_configured, enabled, model_count]
      properties:
        id:
          type: string
        provider_type:
          type: string
        base_url:
          type: string
        credential_configured:
          type: boolean
        enabled:
          type: boolean
        timeout_millis:
          type: integer
          format: int64
        model_count:
          type: integer
        health_status:
          type: string
        test_status:
          type: string
        cost_config_status:
          type: string
        route_policy_hints:
          type: array
          items:
            $ref: "#/components/schemas/AdminRoutePolicyHint"
        models:
          type: array
          items:
            $ref: "#/components/schemas/AdminChannelModelView"
    AdminChannelModelView:
      type: object
      required: [public_model, upstream_model]
      properties:
        public_model:
          type: string
        upstream_model:
          type: string
        capabilities:
          type: array
          items:
            type: string
        supported_parameters:
          type: array
          items:
            type: string
        health_status:
          type: string
        test_status:
          type: string
        cost_config_status:
          type: string
        metadata:
          type: object
          additionalProperties: true
    AdminRoutePolicyHint:
      type: object
      required: [route_id, public_model, enabled, priority, weight]
      properties:
        route_id:
          type: string
        public_model:
          type: string
        strategy:
          type: string
        enabled:
          type: boolean
        priority:
          type: integer
        weight:
          type: integer
    AdminChannelUpsertRequest:
      type: object
      properties:
        id:
          type: string
        provider_type:
          type: string
        base_url:
          type: string
        api_key:
          type: string
          writeOnly: true
        credential_ref:
          type: string
        enabled:
          type: boolean
        timeout_millis:
          type: integer
          format: int64
        models:
          type: array
          items:
            $ref: "#/components/schemas/AdminChannelModelInput"
    AdminChannelCredentialRotationRequest:
      type: object
      required: [api_key]
      properties:
        api_key:
          type: string
          writeOnly: true
    AdminChannelModelInput:
      type: object
      required: [public_model, upstream_model]
      properties:
        public_model:
          type: string
        upstream_model:
          type: string
        capabilities:
          type: array
          items:
            type: string
        supported_parameters:
          type: array
          items:
            type: string
        health_status:
          type: string
        test_status:
          type: string
        cost_config_status:
          type: string
        metadata:
          type: object
          additionalProperties: true
    AdminChannelTestResult:
      type: object
      required: [channel_id, status, message, credential_configured, model_count, tested_at]
      properties:
        channel_id:
          type: string
        status:
          type: string
        message:
          type: string
        credential_configured:
          type: boolean
        model_count:
          type: integer
        tested_at:
          type: string
          format: date-time
    AdminChannelSyncRequest:
      type: object
      required: [upstream_models]
      properties:
        channel_id:
          type: string
        provider_type:
          type: string
        upstream_models:
          type: array
          items:
            $ref: "#/components/schemas/AdminChannelModelInput"
    AdminChannelSyncPreview:
      type: object
      required: [channel_id, unchanged]
      properties:
        channel_id:
          type: string
        provider_type:
          type: string
        added:
          type: array
          items:
            $ref: "#/components/schemas/AdminChannelModelPreviewItem"
        removed:
          type: array
          items:
            $ref: "#/components/schemas/AdminChannelModelPreviewItem"
        changed:
          type: array
          items:
            $ref: "#/components/schemas/AdminChannelModelPreviewItem"
        unchanged:
          type: integer
        warnings:
          type: array
          items:
            type: string
    AdminChannelModelPreviewItem:
      type: object
      required: [public_model, known_catalog_model, customer_price_configured]
      properties:
        public_model:
          type: string
        upstream_model:
          type: string
        current_upstream_model:
          type: string
        health_status:
          type: string
        test_status:
          type: string
        cost_config_status:
          type: string
        known_catalog_model:
          type: boolean
        customer_price_configured:
          type: boolean
    AdminChannelSyncApplyResult:
      type: object
      required: [channel_id, applied_at, preview, channel]
      properties:
        channel_id:
          type: string
        applied_at:
          type: string
          format: date-time
        preview:
          $ref: "#/components/schemas/AdminChannelSyncPreview"
        channel:
          $ref: "#/components/schemas/AdminChannelView"
    AdminChannelHealthEventListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/AdminChannelHealthEvent"
    AdminChannelHealthEvent:
      type: object
      required: [id, channel_id, status, source, message, observed_at]
      properties:
        id:
          type: string
        channel_id:
          type: string
        status:
          type: string
        source:
          type: string
        message:
          type: string
        observed_at:
          type: string
          format: date-time
    ErrorEnvelope:
      type: object
      required: [error]
      properties:
        error:
          $ref: "#/components/schemas/ErrorBody"
    ErrorBody:
      type: object
      required: [code, message, type]
      properties:
        code:
          type: string
        message:
          type: string
        type:
          type: string
        retryable:
          type: boolean
        request_id:
          type: string
